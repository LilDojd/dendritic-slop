name: Check

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            all-systems: true
          - os: macos-15
            all-systems: false

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3

      - name: Check formatting
        if: runner.os == 'Linux'
        run: nix fmt -- --ci

      - name: Evaluate every supported system
        if: matrix.all-systems
        run: nix flake check --no-build --all-systems --keep-going --no-update-lock-file

      - name: Build native checks
        run: nix flake check -L --no-update-lock-file
